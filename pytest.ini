[pytest]
testpaths = scripts/tests
pythonpath = scripts
markers =
    slow: regenerates the calibration block of stats-vectors.json (about two minutes); off by default, run with -m slow
addopts = -m "not slow"
